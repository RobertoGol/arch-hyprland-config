# 🚀 Быстрый старт

## Для пользователя (в Live-среде Arch)

Скопируйте и выполните **одну команду** в терминале:

```bash
pacman -Sy git --noconfirm && \
git clone https://github.com/RobertoGol/arch-hyprland-config.git && \
cd arch-hyprland-config && \
chmod +x all_in_one_install.sh && \
./all_in_one_install.sh
```

**Всё остальное произойдёт автоматически!**

---

## Что произойдёт:

1. ✅ Установится git
2. ✅ Склонируется репозиторий
3. ✅ Запустится скрипт установки
4. ✅ Автоматически создастся пользователь: **Archlinux / Archlinux**
5. ✅ Автоматически создастся root: **root / root**
6. ✅ Автоматически установится hostname: **arch-hypr-rj**
7. ✅ Автоматически разметится и отформатируется `/dev/sda`
8. ✅ Установится Arch Linux + Hyprland + Firefox Developer Edition
9. ✅ Скопируются все конфиги в `~/.config`
10. ✅ Настроятся оптимизации производительности
11. ✅ Система перезагрузится

---

## ⚠️ ВАЖНО:

- Скрипт **полностью стирает `/dev/sda`** без подтверждений
- Используйте только на тестовых ВМ или чистом диске
- После установки настройте Boot Order в VMware (диск должен быть выше ISO)

---

## После перезагрузки:

1. Войдите под пользователем: **Archlinux / Archlinux**
2. Запустите Hyprland: `Hyprland`
3. Готово! 🎉

